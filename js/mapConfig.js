// Единственный переключатель для отката "карта города как главный экран":
// поставь false — и всё вернётся ровно к прежнему поведению (профиль +
// нижняя навигация, кнопка "Помощь" снова в профиле), ни строчки
// остального кода трогать не придётся. Вынесен в отдельный файл (а не
// просто экспортирован из shell.js), чтобы избежать циклического импорта —
// shell.js сам импортирует profile.js, а profile.js тоже должен знать
// значение этого флага.
export const USE_TOWN_MAP_HOME = false;
